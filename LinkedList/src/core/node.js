
// simple class node with element as added value and next as subsequent node 
class Node {
    // constructor 
    constructor(element) {
        this.element = element;
        this.next = null
    }
}

module.exports = Node;